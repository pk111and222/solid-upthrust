# <组件 / 能力名称>

状态：规划 / 实现中 / 已实现待验证 / 已验证（按事实选择）

## 契约

- 能力 ID：`<component>.<capability>.<scenario>`
- 目标行为：
- 输入与默认值：
- 受控/非受控与事件参数、时序：
- 边界、异步与清理：
- 对标版本/来源（如适用）：
- 已支持 / 不支持 / 尚未验证的差异：

## 文件与覆盖映射

| 能力 ID | 源码与公开类型 | example 演示 | docs 页面 / 示例 | L1 | L2 | L3 | L4 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `<id>` | 待补 | 待补 | 待补 | 待验证 | 待验证 | 待验证 | 待验证 |

每个测试格填写 `<层>/<物料名>/<能力模块>` 下的真实文件和用例名；不适用写原因。不要把“待接入”写成“不适用”。

## 验证记录

| 命令 | 结果 | 未通过 / 未运行原因 |
| --- | --- | --- |
| 目标测试 | 未运行 | |
| 全量回归 | 未运行 | |
| 类型检查 | 未运行 | |
| 相关构建 | 未运行 | |
| 相关真实浏览器测试 | 未运行 | |

## 未决项

- 当前不能承诺的能力：
- 基线遗留问题：
- 下一步 / 需要的人工配置：
